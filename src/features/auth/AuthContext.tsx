// Re-export from the canonical location for backwards compatibility
export { AuthProvider, useAuth, type UserRole, type Profile } from '@/contexts/AuthContext';
export type { default } from '@/contexts/AuthContext';
